# Create the esm3 embeddings of pdbbind2020 sequence`s`
# Create the denv2 2fom embeddings for 2fom sequence
# Rank the distances and take first few example of th sequences.
# Based on the selected values, rank based on the tanimoto similarity of the ligands.
# Plot them on the graph (scatter).